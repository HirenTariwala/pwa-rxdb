import { Badge as BadgeComponent } from "@mui/material";
import { BadgeProps } from "@mui/material";
const Badge = (props: BadgeProps) => {
  return <BadgeComponent {...props}></BadgeComponent>;
};
export default Badge;
