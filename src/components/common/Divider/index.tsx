import { Divider as DividerComponent } from "@mui/material";
import { DividerProps } from "@mui/material";
const Divider = (props: DividerProps) => {
  return <DividerComponent {...props}></DividerComponent>;
};
export default Divider;
